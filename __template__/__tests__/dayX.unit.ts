import os from "os";
import { calculatePartOne, calculatePartTwo } from "../dayX";

describe("Part 1", () => {
  it("returns X given the example input", () => {
    const input = [].join(os.EOL);

    expect(calculatePartOne(input)).toEqual(null);
  });
});

describe("Part 2", () => {
  it("returns X given the example input", () => {
    const input = [].join(os.EOL);

    expect(calculatePartTwo(input)).toEqual(null);
  });
});